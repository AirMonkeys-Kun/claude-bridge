# ── Runtime results ──
r_*.json
r_*.txt

# ── Logs ──
*.log
watcher/*.log

# ── Cluster runtime artifacts ──
cluster/.heartbeat
cluster/*/.heartbeat
cluster/*/.lock
cluster/*/queue.txt
cluster/*/.watcher.lock
cluster/*/.watcher_heartbeat
cluster/.scheduler_heartbeat
cluster/*/.user_bridge_started
cluster/*/worker_stderr.log
cluster/global_state.json
cluster/state_history.json

# ── Legacy watcher runtime ──
watcher/.watcher.lock
watcher/.watcher_heartbeat

# ── SQLite temp DBs ──
watcher/_DIPS.sqlite
watcher/_WebStorage.sqlite
*_DIPS.sqlite
*_WebStorage.sqlite

# ── Python bytecode ──
__pycache__/
*.pyc
*.pyo

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Temp / scratch txt files ──
watcher/*.txt
!watcher/queue.txt

# ── OS junk ──
ehthumbs.db
Desktop.ini
